# keystore.properties — NÃO commitar este arquivo (está em .gitignore)
# Copie para keystore.properties e preencha com seus valores reais.
storeFile=../keystore/release.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
